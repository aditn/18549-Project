import React from 'react';

const EmptyView = React.createClass({
  render() {
    return (
      <div className="empty-view">
        Opps, Nothing to show
      </div>
    )
  }
});

export default EmptyView;
